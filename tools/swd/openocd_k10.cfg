# K10 V2.1 (GD32F303CBT6) via Raspberry Pi Pico debugprobe
# 読み出し専用の用途を想定。reset はかけず halt のみ。
source [find interface/cmsis-dap.cfg]
transport select swd
adapter speed 1000

# GD32F303 は Cortex-M4 なので STM32F1 の既定 IDCODE と一致しない。0 = 照合しない
set CPUTAPID 0
source [find target/stm32f1x.cfg]
